# Progress Log

## 2026-03-28 ~ 2026-03-30

### 一、项目主线：Local RAG Doc QA
这几天主要围绕本地 RAG 文档问答项目推进，目标是把项目从“最小可跑 demo”逐步推进到“可展示、可写进简历、可继续扩展”的状态。

---

## 二、已完成的核心工作

### 1. 跑通最小 RAG 主链
已完成以下核心模块，并能够串成完整问答流程：

- 文档读取：`loader.py`
- 文本切分：`chunker.py`
- embedding：`embedder.py`
- 向量存储：`vector_store.py`
- 检索：`retriever.py`
- 重排：`reranker.py`
- Prompt 构造：`prompt_builder.py`
- 问答主流程：`rag_pipeline.py`
- FastAPI 接口：`/ask`

当前主链已经能够实现：

Document -> Chunk -> Retrieval -> Rerank -> Prompt -> LLM -> Answer

---

### 2. 完成本地索引同步机制
为了避免每次都无脑全量重建，已经实现本地索引同步逻辑。

已加入：

- `doc_registry.json`：记录文档级状态
- `chunks.jsonl`：记录 chunk 级数据
- `sync_index()`：负责同步文档新增、修改、删除后的 chunk 索引

当前同步思路为：

- 文档新增：新建 chunk 和 registry
- 文档未变化：复用旧 chunk
- 文档已变化：重建该文档 chunk
- 文档已删除：从新索引结果中自然消失

这一步让我第一次真正接触到“本地持久化 + 状态同步 + 数据一致性”这类工程问题。

---

### 3. 完成 `doc_id` 贯穿全链路
已将 `doc_id` 贯穿到：

- `Document`
- `Chunk`
- `RetrievedChunk`
- `SourceItem`
- `AskResponse.sources`

并明确区分了：

- `doc_id`：系统内部主键，使用相对路径
- `source`：给用户展示的来源名称

这一步解决了后续做索引同步、source 返回和子目录文档管理时的核心问题。

---

### 4. 完成本地大模型接入
已成功接入本地 Ollama 模型，并完成 `/ask` 的真实问答，不再是 mock answer。

已确认跑通：

- 本地生成模型：`qwen3:8b`
- 本地 embedding 模型：`qwen3-embedding:0.6b-fp16`

当前项目已经不是“假流程演示”，而是真正的：

- 本地 embedding 检索
- 本地大模型生成
- FastAPI 服务化问答

---

### 5. 升级检索能力：从自写哈希 embedder 升级到真实 embedding
一开始的 embedding 是自写的简化版本，主要问题是：

- 对中文非常不友好
- 只能做很粗糙的词面匹配
- 对短中文问题命中率差

后续已切换到 **Ollama embeddings**，使用 `qwen3-embedding:0.6b-fp16` 做真实语义向量检索。

这个升级带来的变化很明显：

- 中文问题命中率明显提升
- 叙事型文本问答效果明显改善
- 项目从“演示级检索”升级到“真实语义检索”

---

### 6. 完成中文测试集扩展
测试集不再只使用模型本来就懂的公开技术常识，而是切换到更适合 RAG 测试的中文知识库文档，包括：

- 项目规则文档
- 接口说明文档
- 部署说明文档
- 权限/流程文档
- 干扰项文档
- 中文叙事型文本

这样做之后，项目测试更能验证：

- 检索是否真的有效
- 模型是否基于资料回答
- 无关问题是否会拒答
- 中文问答是否可靠

---

## 三、已解决的重要问题

### 1. 修复空查询 500
此前空字符串会导致 `500 Internal Server Error`。  
现在已经修复为：

- 空查询 / 纯空格
- 直接返回 `400`
- detail 为：`query cannot be empty`

这一步提升了接口鲁棒性。

---

### 2. 解决路由未生效与端口占用问题
排查并解决了：

- `/ask` 改动没有生效
- 实际不是当前服务在响应
- 8000 端口被旧 uvicorn/python 进程占用
- `--reload` 导致服务重载不稳定

目前已经可以稳定启动服务并正常进入 `/docs`。

---

### 3. 修复本地索引文件问题
排查并处理了多个持久化相关错误，包括：

- 空的 `doc_registry.json` 导致 `JSONDecodeError`
- `chunks.jsonl` 中坏数据导致 `Chunk` 校验失败
- 文件路径和目录路径混淆
- `json.load / loads / dump / dumps` 混用

这部分虽然花了时间，但让我真正把 JSON / JSONL / 路径 / 文件读写这些基础工程能力补了起来。

---

### 4. 修复 `sources` 类型错误
曾出现 `AskResponse.sources` 类型与实际返回对象不一致的问题。  
后续已明确：

- 内部检索对象使用 `RetrievedChunk`
- API 返回对象使用 `SourceItem`

避免了把内部对象直接塞进响应模型导致的校验错误。

---

## 四、当前项目效果

### 当前已经能做到：
- 本地启动 FastAPI 服务
- `/ask` 走完整 RAG 主链
- 使用本地 embedding 做中文检索
- 使用本地 `qwen3:8b` 做生成
- 返回 `answer + sources`
- 空查询返回 400
- 无关问题会回答“不知道”
- 中文叙事型文本可抽取具体事实

### 当前效果示例
例如测试：

- “谁是美国总统？”  
系统会回答资料不足，不乱编。

- “沈砚去旧书店是为了什么？”  
系统能够从中文叙事文本中抽取出较具体、较准确的事实。

这说明项目已经不只是“技术概念问答”，而具备了真正的文档理解和检索增强能力。

---

## 五、这几天学到的关键东西

这几天最大的收获不是“又多写了几个函数”，而是以下几类工程能力开始真正落地：

### 1. 模块串联能力
不再只是会看单个函数，而是开始能理解并修改：

- schema
- service
- pipeline
- API 返回结构

之间的联动关系。

### 2. 本地持久化能力
第一次较系统地接触并实践了：

- JSON
- JSONL
- 路径管理
- 本地索引文件
- 文档状态同步

### 3. AI 应用工程意识
开始明确知道一个本地 RAG 系统真正的关键不只是“大模型会不会回答”，而是：

- 文档怎么进系统
- 检索怎么做
- sources 怎么返回
- 什么时候该说不知道
- 如何做本地索引同步
- 如何把能力封装成 API

### 4. 问题定位能力
现在遇到 500 不再只盯着“为什么是 Internal Server Error”，而是会去追：

- 路由有没有生效
- traceback 最后一段是什么
- 是 JSON 文件问题
- 还是 Pydantic 校验问题
- 还是端口占用 / reload / 旧进程问题

这部分进步很大。

---

## 六、当前仍未完成的点

项目虽然已经进入“可展示”阶段，但还没有彻底收尾。当前还需要继续补：

- `sources` 返回进一步收紧，减少弱相关噪声
- README 第一版正式写好
- requirements / 运行说明整理
- GitHub 提交历史继续规范
- embedding 持久化（当前重启仍会重新建向量）
- 后续可考虑 Docker 化
- 后续可考虑更强的 reranker

---

## 七、下一步计划

### 短期下一步
1. 收紧 `sources` 返回逻辑
2. 完成 README 第一版
3. 更新 GitHub
4. 固化一组可展示测试用例

### 中期下一步
1. 考虑 embedding 持久化
2. 视情况增加 PDF 等格式支持
3. 评估是否继续做一个最小 Agent / Tool Calling demo
4. 为简历和面试准备项目讲法

---

## 八、当前阶段总结

这几天最大的意义是：  
我已经把项目从“空想阶段”推进到了“真实本地 AI 应用工程 demo 阶段”。

当前项目已经具备：

- 本地模型
- 本地检索
- 中文问答
- 来源返回
- 索引同步
- API 服务化

这说明我已经不只是“懂一些概念”，而是在开始具备把 AI 能力做成系统的能力。

接下来最重要的不是继续无限扩展，而是：

**收尾、整理、展示、沉淀成真正可投递的项目成果。**