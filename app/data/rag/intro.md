# RAG 简介

RAG 是 Retrieval-Augmented Generation 的缩写，中文通常翻译为“检索增强生成”。

一个典型的 RAG 系统通常包含以下步骤：

1. 加载文档
2. 将长文档切分为多个 chunks
3. 对 chunks 进行向量化表示
4. 根据用户问题检索相关 chunks
5. 将检索结果拼接到 prompt 中
6. 交给大模型生成最终答案

RAG 的核心价值不在于让模型“记住更多知识”，而在于让模型在回答问题时能够参考外部资料，从而减少无依据编造。